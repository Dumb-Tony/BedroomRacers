# 11 — User Interface

## Principles

- **Colourful, legible, unobtrusive.** In that order of priority when they conflict.
- **Readable at mobile size first.** If it works on a phone it works everywhere.
- **Diegetic where possible.** Stickers, notebook labels, cardboard signs — not
  glass-and-neon sci-fi panels.
- **No text where an icon will do**, especially for the 8-year-old audience.
- **Nothing obscures the racing line.** HUD lives in corners.

## Visual style

UI elements may resemble stickers, toy packaging, notebook labels, crayon drawings,
plastic buttons, cardboard signs.

The unifying idea: the interface looks like it was made by the same child who owns
the toys — hand-cut, hand-lettered, slightly wonky. Nothing perfectly aligned.

> **Built (Phase 12).** See *The interface came in the box* below. Everything on
> screen is now made of four materials and nothing else, through one shared kit
> (`BR.Toy`, in `src/ui/HUD.js`).

## HUD

During a race:

| Element | Position | Notes |
| --- | --- | --- |
| **Position** | Top left | Large. `3rd` / `1st`. The most-read element |
| **Lap count** | Top left, under position | `LAP 2/3` |
| **Timer** | Top centre | Current time; lap split flashes on lap completion |
| **Boost meter** | Bottom centre or near vehicle | Must be readable without looking away from the car |
| **Item slot** | Top right | Empty when items are disabled |
| **Mini-map** | Bottom right | Track outline, player dot, opponent dots |
| **Objective** | Contextual | Only when relevant to the mode |

### Boost meter placement

The hardest HUD decision. A corner-mounted meter forces the eye off the car at
exactly the moment precision matters.

Options: attached to the vehicle (a ring or trailing bar), bottom-centre near the
natural eye position, or both — a subtle vehicle-attached cue plus a precise corner
readout.

**Recommend a vehicle-attached primary cue.** Resolve during Phase 1, since drift
charge feedback is part of whether drifting feels good.

### Next-corner indicator

Bottom-centre, just under the car — corners are read while looking at the road,
not at the corner of the screen. A curved arrow shows which way the road bends,
how sharply, and a bar that fills as it arrives.

**Derived from the racing line the AI already uses.** Every waypoint carries a
`targetSpeed` computed from curvature (`05_Tracks.md`), so severity comes free
and cannot disagree with the track — there is no second copy of the corner data
to fall out of sync.

- Colour and shape by severity: green, amber, red. Only the worst tier gets a
  word; labelling every bend is noise.
- It fades in as the corner comes into range, so a distant one does not shout.
- Nothing is drawn on a straight.
- Distance is a **filling bar, not a number** — world units mean nothing to a
  player.

It earns its place because of the camera: at 17.5° you can see a long way ahead
but the ground is compressed to 30%, so a corner reads late. This buys that time
back.

Verified against both tracks: **41 corners, 100% direction agreement** with the
geometry measured independently, severity tracking the real bend angle (a −82°
turn reads as a hairpin, −24° as easy), nothing shown on the longest straight,
and each viewport in a split tracking its own driver.

### Callouts

Large, brief, animated text over the action. Toy-sticker styling.

`Great Drift!` · `Shortcut!` · `Perfect Landing!` · `Final Lap!` · `New Best Time!`

Callouts are the cheap version of an announcer (see `13_Audio.md`) and should work
with sound off.

## Screens

### Main menu
Play · Garage · Track Select · Collection · Settings · Credits

Presented as objects in the room rather than a list where practical.

### Garage
See `09_Vehicles.md`. A toy shelf or carrying case.

### Track select
The bedroom seen from above. Physical areas act as regions: town rug, under the bed,
toy box, desk, window area, closet, and future environments.

Event nodes sit on the map. Locked content is visible but greyed — players should see
what's coming.

### Results
Finishing position · race time · best lap · medal earned · stars completed ·
collectibles found · rewards unlocked · **Retry** · **Next event**

Retry must be the most prominent button and must be reachable in one press. Per
`01_Game_Loop.md`, retry-to-countdown is targeted under 2 seconds.

### Pause

> **Built.** Resume · Restart · Quit to Events, plus the settings worth reaching
> mid-race: SFX volume, music volume, auto-accelerate.

Available at any time, including mid-air and during the countdown — verified.

**Quitting lives here rather than on a key.** Escape used to abandon a race
outright with no confirmation, which is a lot of lost progress for a mistyped
key. Escape now pauses, and quitting is a deliberate second action.

**The overlay is full-screen and drawn once**, not per viewport. Pause is one
global state, and in split screen any player can call it — four separate pause
cards would imply four separate pauses.

**Audio goes quiet while paused.** Nothing is moving, so a held engine tone would
sit there droning at whatever speed the car happened to be doing.

Volume is a **click-position slider**: where you click is the value. No drag
state, and no second interaction model on the canvas. Changes write through to
the audio graph and to the save immediately.

Settings shown here are the ones a player wants mid-session. The full tuning
surface stays in the debug panel, which is Phase 1 tooling and due for deletion.

## Accessibility

Not optional, and not deferred. Several of these are load-bearing for the 8+ audience.

- Adjustable sound levels (music and SFX separately)
- Reduced screen shake
- **Reduced camera rotation** — see below
- Reduced flashing effects
- Colourblind-friendly indicators
- High-contrast UI mode
- Remappable controls
- Auto-accelerate (default on)
- Steering assistance
- Difficulty selection
- Subtitle support
- Pause at any time

### Notes

**Colourblind indicators** matter more than usual here — vehicles are identified by
colour name (Red Racer, Blue Buggy). Never rely on colour alone for position
indicators or opponent identification; pair with shape or number.

**Steering assistance** should be a gentle nudge away from walls, not auto-steering.
Aim it at the youngest players without making the game play itself.

**Reduced camera rotation** is new, and matters more than it sounds. The chase
camera rotates the whole world around the car (`03_Driving_Physics.md`). A
rotating view is harder to parse for younger players and is a known motion-sickness
trigger. The option should clamp or disable yaw rotation, falling back toward a
fixed-orientation view. Cheap to build — the projection already takes yaw as a
parameter, so the setting is a clamp on one number.

**Reduced flashing** interacts with boost effects, callouts and the holiday
string-lights lighting state.

## Onboarding

Track 1 is the tutorial (`06_World_Town_Rug.md`). Teaching is diegetic — printed
signs and rug arrows, not modal popups.

Rule: **a player who ignores every prompt must still be able to finish the race.**
Instruction is an aid, never a gate.

### The solo player was never told the controls (Phase 9)

Found while preparing the game to be sent to somebody outside the project, which
is exactly the kind of thing only that occasion finds.

Control labels were drawn under each half of a **split screen** — gated behind
`view.label`, which is null with one player. So the seat most likely to belong
to someone seeing the game for the first time was the only one told nothing at
all. `Input.LABELS` had no `solo` entry to draw even if it had tried, and **solo
bindings are not P1's**: solo drifts on Space and boosts on Shift, where P1
drifts on R-Shift and boosts on Enter. Borrowing P1's label would have taught
keys that do nothing, which is worse than silence.

Two places now, and neither is a popup:

- **Across the countdown**, fading out over 2.2s once the lights go out. The
  countdown is dead time — you cannot drive, and nothing else occupies the lower
  screen — so it costs a returning player nothing and is gone before the first
  corner.
- **On the main menu**, permanently and quietly, so a player who has forgotten
  which key drifts has somewhere to look without a screen to open.

Both show the gamepad line instead when a pad holds seat one.

Verified by pixels rather than by reading the code back, because the whole claim
is about something reaching the screen: the hint occupies 9,494 pixels during
the countdown, 49% of its intensity halfway through the fade, and **exactly zero
pixels and zero intensity** once racing.

That measurement took three attempts, and the first two both lied:

- **Counting lit pixels in the strip** returned ~32,000 whether the hint drew or
  not — the rug, the lap counter and the boost meter all live down there, and a
  line of text is invisible inside that. Replaced by rendering the same frame
  twice, once with the hint suppressed, and diffing.
- **Diffing with a real `dt`** then reported 1,080 hint pixels at a moment the
  hint had already returned early, because the renderer eases the camera by `dt`
  and the diff counted that motion as if it were the text. Rendering with
  `dt = 0` freezes everything that is a function of time.

A self-check that two identical renders differ by zero now runs both on the grid
**and** with the car moving. The first version only checked it stationary, which
is precisely why it missed the second fault.

### Touch — the game shipped a public link before it could be played on a phone

This document's first line has always been *"readable at mobile size first — if
it works on a phone it works everywhere"*. Then a link went out, and on a phone
you could browse the ladder, choose an event, start a race, and discover there
was **no way to steer**. `Input` listened for keydown, keyup and gamepads and
nothing else; the menus only worked because `Screens` uses pointer events for
its hit regions.

That is the worst shape a gap can have — it looks like it works right up to the
moment it matters.

`Touch.js` adds on-screen controls. It is **not a second control scheme**: it
produces the same booleans a key produces and hands them to the same
`Input.sample`, so the vehicle controller, the AI and every measured target time
are untouched. A tilt or swipe scheme would have been a different game to tune.
It drives **seat one only** — four thumbs on one phone is not a thing, so the
split-screen seats stay on keys and pads.

Auto-accelerate is on by default, so a thumb never holds a throttle: left thumb
steers, right thumb drifts and boosts, item appears only while something is
held, and there is a pause button because **a phone has no Escape key** — without
one a race could only be left by finishing it or reloading the page.

Three things the harness caught, none of which throws:

- **The left pad hung off the screen.** Its centre was placed *at* the margin
  rather than a radius inside it, so a 45-unit circle started at −20.
- **Adjacent buttons shared a touch area.** Hit boxes are deliberately larger
  than the drawn circles — a missed steering input is the difference between a
  corner and a wall — but `hit` returns the first match, so overlapping slop
  meant a thumb aimed at RIGHT silently got LEFT. Spacing is now derived from
  the slop constant instead of guessed, and the radius is solved for the widest
  that fits the viewport rather than taken from the short edge alone.
- **Lifting one finger cleared the others**, until the state was rebuilt from
  all held pointers rather than toggled.

Verified with real `PointerEvent`s dispatched at the buttons' actual screen
positions, not by setting the state object directly: that it worked was never in
doubt, only whether a finger landing on a drawn button reaches it. A mouse press
deliberately does not activate the pads, so a desktop with a touchscreen is not
covered in thumb targets.

### The menu did not fit a phone either

Found in the same pass. The button stack **started** proportionally at
`h * 0.44` and then advanced in **fixed pixels** — a mix that cannot hold as a
screen shortens. At 812x375 the difficulty row drew past the bottom edge, so
difficulty could not be changed at all.

The gaps now scale, but only when they have to: above about 700 tall the factor
is 1 and the layout is pixel-for-pixel what it was. Verified by content bottom —
desktop 564 and iPad 550, unchanged, while the phone sizes came in from 377 to
319. **Button heights are deliberately not scaled**, because they are touch
targets and shrinking those to win space ruins the menu on exactly the devices
it was shrunk for.

## Open questions

1. ~~Boost meter placement.~~ **RESOLVED in Phase 1: bottom left, under the
   speed readout** (`HUD.js`), as a bar that changes colour rather than only
   filling — blue below `boostMinToFire`, green once it can be spent, amber
   while firing. The state that matters is "can I use this yet", and a fill
   level alone does not say it.
2. **Partly resolved.** ~~Does the mini-map rotate with the camera or stay
   north-up?~~ **It stays north-up.** The world already rotates around the car,
   so a rotating map would show the same orientation as the screen and tell the
   player nothing new — while leaving no fixed reference anywhere in the
   interface. Held fixed it does the one job the rotating view cannot: shows
   where you are in the circuit as a whole.

   The player is an **arrowhead, not a dot**, so facing is readable. Opponents
   are dots in their own body colours, matching how they are identified on
   track. The toy train is shown deliberately — it is meant to be timed, not
   stumbled into.

   **Both now exist.** The next-corner indicator is built too, and both have a
   slider reaching zero, so which earns its screen space is answerable by
   driving rather than arguing.

   They turned out **not to be alternatives**. The map answers *where am I in
   the lap*; the corner hint answers *what happens in the next second*. Nothing
   yet says a track needs both, but they are not competing for the same job.
3. ~~Menu navigation model~~ **RESOLVED: full keyboard and gamepad support, not
   pointer-only** — and it was retrofitted, exactly as the question feared it
   would have to be. A player holding a pad could race but could not choose an
   event or leave the pause menu. See the section below.

## Lists must scroll before the content outgrows the screen

The event list drew every event from the top of the screen downwards with no
clipping and no scroll. That was fine at three events and invisible at six. At
**fourteen**, across four worlds, nine of them were drawn off the bottom of the
screen with no way to reach them — including the entire stunt world. A player
could not race a loop because they could not see that the event existed, and
nothing about the screen suggested there was anything below the last visible row.

Now: a clipped viewport, wheel / drag / arrow-key / PageUp-Down / Home-End
scrolling, a scrollbar, and a one-line "scroll for more" prompt while the list is
at the top. Rows outside the band are not drawn **and not registered as hit
regions** — a button drawn off-band is still clickable otherwise.

The scroll position survives returning from a race, so finishing an event does
not throw you back to the top of a fourteen-row list.

**Check every list against the content it will hold, not the content it holds
today.** The garage roster and the results card have the same shape of risk.

## The interface came in the box (Phase 12)

The world had its material pass: the cars are moulded plastic with a seam and a
lit flank, the rug is woven pile printed with a town, the stunt deck is varnished
floorboards. **The interface was the last thing in the game that still looked
like a dev tool** — flat translucent black rectangles, monospace text, and a
mini-map in a grey box.

The direction, chosen explicitly and knowing it was the most work and carried the
most readability risk: *the menu looks like it came in the box with the cars.*

### Four materials, and nothing else

One kit, `BR.Toy`. It lives at the top of `src/ui/HUD.js` because that is the
first of the four UI files to load in both `index.html` and
`tools/build-artifact.sh`, and a fifth file would mean editing a build manifest
outside `src/ui/` for a pure art change.

| | is | carries |
| --- | --- | --- |
| **plastic** | `plate` | housings, buttons, tabs, the mini-map frame, sliders |
| **paper** | `sticker`, `banner`, `stamp`, `tape` | labels, callouts, the countdown, the logo |
| **card** | `card` | the event-select box lid, the event rows, the scorecard |
| **ink** | — | what is printed on the paper |

A plate is the same five things a car got in Phase 12 (`12_Art_Guide.md`): a dark
outline round the silhouette, a body lighter at the top than the bottom, a lit
hairline inside the top edge, a shaded one inside the bottom, and a shadow behind
it. `press` sinks a part into its own socket and swaps the light; `sunk` is a
channel moulded into a bigger part.

### One light, and it is NOT the room's

`Projection.light` is a **world** light — the camera rotates with travel, so
shadows sweep across the screen as you corner, which is what a room does.

The interface is not in the room; it is stuck to the glass. A HUD plate lit by
the world light would swing its highlight round the screen every time the player
turned a corner, and the whole interface would read as a turntable — the exact
failure `Projection.js` warns about, one layer up. **`Toy` has its own light,
fixed in screen space, up and to the left**, and every bevel, shadow and sticker
in the game agrees with it.

### Nothing that can be pressed is ever rotated

`Screens.button` registers an axis-aligned rect at the coordinates it draws at.
A rotated button would be clickable somewhere it is not drawn — the same class of
lie as a barrier drawn shorter than it collides. Stickers, stamps and banners
tilt, because none of them is pressable. Buttons, rows, tabs and sliders never
do, however much "nothing perfectly aligned" would like them to.

### Legibility is two pairings and nothing between them

A bright plastic interface fails by putting a mid-tone on a mid-tone. So every
readout is one of exactly two things:

- **dark ink on a cream label** — the numbers read while driving
- **cream moulded on dark plastic** — the words that label them

Measured as WCAG contrast between the 2nd and 98th percentile luminance inside
each readout's own rectangle, on a real frame of Rug Route at ten seconds:

| | before | after |
| --- | --- | --- |
| **position** | 6.33:1 | **11.28:1** |
| lap | 11.76:1 | 11.99:1 |
| clock | 12.11:1 | 12.22:1 |
| speed | 13.27:1 | 12.22:1 |
| boost, charged | 7.41:1 | 10.61:1 |

The old HUD won legibility the cheap way — white on 45% black — so most of it was
already high. **Position is the one that moved**, and it is the element this
document calls the most-read on screen: it was gold on translucent black, and
gold is a mid-luminance colour. It is now near-black ink on a cream label, and
gold is spent on *being in the lead* instead. Speed lost a point of contrast
going from white-on-black to ink-on-cream and is still nearly three times the
4.5 floor.

### Cost

Counted by wrapping every function on `CanvasRenderingContext2D.prototype` for
one frame — a broader wrapper than the one in `12_Art_Guide.md`, so the totals
are not comparable with that document's, only with each other.

| | before | after |
| --- | --- | --- |
| whole race frame | 5,059 | 5,259 |
| world only | 4,814 | 4,814 |
| **the interface** | **245** | **445** |
| — HUD | 55 | 204 |
| — mini-map | 175 | 205 |
| — corner hint | 15 | 36 |

**+200 operations, 3.8% of a frame.** It is drawn per viewport, so a four-way
split pays it four times; that is 1,780 against 980, on a frame that already
draws the world four times.

Render-only, and proved rather than asserted: **all 22 events measure 0.00
against the committed baseline**.

### Three things that had to be got wrong first

- **`Renderer.shade` returns `rgb(...)` and takes `#rrggbb`.** Feeding its own
  output back in gives `parseInt('rgb(46,60,77)', 16)` = NaN, and every channel
  comes out zero. Nothing throws. The boost channel — a shade of the shell handed
  to `plate`, which shades it again for its bevels — was a **black slab** in the
  corner of every race, and worse, `button()` lightens its colour when focused,
  so **the focused button was the darkest thing on screen**: RACE on the main
  menu drew as a black rectangle with dark gold text, at exactly the moment a pad
  player needs to see where they are. `Toy.shade` normalises its input now.
- **A pressed button is not a differently-tinted button.** The first focus state
  only swapped the bevel and lightened the body, and rendered as a button that
  looked very slightly different if you had the other one to compare it against.
  It is a real socket now — a dark recess with a lit rim, and the body sitting
  1.5px down inside it. The drawn body is 3px smaller than its hit region, which
  is the safe direction: the target does not shrink, the pixels move inside it.
- **A cream disc fades worse than a white glyph.** The countdown was a 96px solid
  numeral at a 0.35 alpha floor and survived it. As ink on a paper disc, *both*
  halves of the contrast fade together and what is left is a smudge on a grey
  road. The floor is 0.62.

### Small screens: an overlap, then an overflow

Rendering the menu at 820x420 — the size this document already records a
destructive bug at — found two more, both older than this pass and both invisible
in the code:

- **The header was drawn through the RACE button.** The title block was pinned at
  `h * 0.22` and ran 110px below it while the stack started at `h * 0.44`, and
  0.22 + 110/h passes 0.44 as soon as `h` drops under about 500. You can read the
  "←" of the control hint on one side of the RACE button and the "m" on the
  other. The header is clamped above the stack now; above about 620 tall nothing
  moves.
- **The save-code row ran off the bottom.** The earlier fix stopped the row
  *overlapping* the difficulty tabs by pushing it down to `by + 70`; on a 420-tall
  screen that puts its bottom edge at 435, so COPY MY CODE, USE A CODE and RESET
  PROGRESS were each sliced in half by the window edge and none could be pressed.
  The same fault turned inside out. It is clamped at both ends now, and the
  difficulty blurb — the only line here that is not a control — is dropped when
  there is honestly no room for both.

**The armed reset is no longer a row at all.** It needs two lines and two buttons
below a difficulty row that already ends at 375 on a 420-tall screen, and every
arrangement of it either sat on the difficulty tabs or ran off the edge. It is a
centred warning plate, which fits at every size there is, and a confirmation that
erases everything ought to stop the screen anyway. It pushes the same two regions
in the same order, so keyboard focus is unchanged.

**The event rows have two shapes.** At 375 wide the card is 327 across and the
RACE button owns the right 106 of it, so the blurb and the second objective
column were drawn straight through the button. Condensing text to fit is the
obvious fix and a bad one — a blurb squeezed to half width is present and
unreadable. A narrow card gets a taller row instead: one objective column, and
the RACE button full width along the bottom, which is a better thumb target
anyway. Every event has exactly three objectives, so the tall row is sized for
three and cannot be outgrown by content.

### Focus order, checked rather than assumed

Focus is an index into `regions`, rebuilt every frame in draw order, so an art
pass that inserts a decoration in the wrong place silently reorders what the
D-pad reaches next — and the region *count* staying the same proves nothing,
because a swap keeps the count.

The action list was dumped before and after on five screen-and-size combinations
plus the pause card. **Identical everywhere**, with one expected difference: at
375x812 the event list registers five rows where it used to register seven,
because the rows are taller. Rows outside the scrolling band are deliberately not
registered as hit regions, and scrolling reaches the rest.

## Related

`01_Game_Loop.md` — loop states these screens serve.
`12_Art_Guide.md` — the visual language.
`02_Mechanics.md` — controls being taught.

## Keyboard and gamepad menus (Phase 8)

Open question 3 answered: **full keyboard and gamepad support, not pointer-only.**

The menus were click-and-drag only, on a game with four-player couch multiplayer
and gamepad driving. A player holding a pad could race but could not choose an
event, pick a car, or leave the pause menu without somebody reaching for a mouse.
That is not a missing convenience; it is a mode of play that did not work.

Focus is an **index into `regions`**, which was already rebuilt every frame in
deterministic draw order — so the index is stable as long as the screen is, and
`set()` resets it because a screen change is the only thing that reorders it. It
reuses the existing `hover` highlight, so the focused control is visible with
nothing new drawn.

Arrows or D-pad move, Enter/Space/A activates, Escape/B goes back. `revealFocus`
scrolls the focused row into the visible band, and `back()` gives every screen an
exit — a pad player who cannot reach BACK is stranded.

Gamepad state is **polled from `draw()`**, because the Gamepad API has no events
and only reports when asked. Every button is edge-detected; a held stick would
otherwise cross the whole list in three frames. Polling is skipped while racing,
where the pad is driving the car.

Verified: all **18 events reachable by keyboard alone**, focus stable across 120
redraws, Escape leads somewhere from every screen, activating a focused row
starts that exact event, and polling with no pad attached is harmless.

> The one flagged "off-band" focus position turned out to be the BACK button,
> which sits below the scrolling band **by design** so it never scrolls away.
> The check was too broad, not the code — worth recording, because the reflex of
> treating a failing check as a defect is what makes bad tests expensive.
