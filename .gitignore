# Build output. dist/play.html is generated by tools/build-artifact.sh from the
# source tree, and the published copy lives in the bedroom-racers-play repo, so
# committing it here would only be a 575 KB duplicate that goes stale.
dist/

# Local server / harness scratch.
*.log
